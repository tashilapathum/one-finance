<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tashila.mywalletfree">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDark"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Insights"></activity>
        <activity android:name=".About" />
        <activity
            android:name=".AccountManager"
            android:label="Account Manager" />
        <activity android:name=".UpgradeToPro" />
        <activity android:name=".AccountDetails" />
        <activity android:name=".NewAccount" />
        <activity android:name=".InitialSetup" />
        <activity
            android:name=".Reports"
            android:label="@string/reports" />
        <activity
            android:name=".ReportsNEW"
            android:label="@string/reports" />
        <activity
            android:name=".EditQuickList"
            android:label="@string/settings_quick_list" />
        <activity
            android:name=".TransactionHistory"
            android:label="@string/recent_transactions" />
        <activity
            android:name=".Settings"
            android:label="@string/settings" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlertReceiver" />
    </application>

</manifest>